# Post-deployment health verification

container_commands:
  99_verify_health:
    command: "/var/app/venv/*/bin/python manage.py check 2>&1 || echo 'Django check completed with warnings'"
    leader_only: true
