import type { FC } from "react";
import { observer } from "mobx-react";
type Props = {
  sprintId: string;
  projectId: string;
};
export const SprintAdditionalActions = observer(function SprintAdditionalActions(_props: Props) {
  return <></>;
});
